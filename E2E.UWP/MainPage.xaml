<Page x:Class="E2E.UWP.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:E2E.UWP"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Name="debugOutput" />
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <CaptureElement Name="preview"
                            Stretch="Uniform"
                            RenderTransformOrigin="0.5,0.5">
                <CaptureElement.RenderTransform>
                    <CompositeTransform ScaleX="-1" />
                </CaptureElement.RenderTransform>
            </CaptureElement>
            <Grid Grid.Row="1">
                <Canvas Name="positionCanvas" />
                <Rectangle StrokeThickness="1" Stroke="Gray" />
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="1" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="1" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Background="Gray"
                          Grid.Row="1"
                          Grid.ColumnSpan="3" />
                    <Grid Background="Gray"
                          Grid.Column="1"
                          Grid.RowSpan="3" />
                </Grid>

                <TextBlock Foreground="Green" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8">
                    <Run Text="{Binding Ms}" />
                    <Run Text=" ms" />
                </TextBlock>
            </Grid>
        </Grid>
    </Grid>
</Page>
